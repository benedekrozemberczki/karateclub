version: 2

build:
   image: latest

python:
   version: 3.7
   system_packages: true
   install:
      - requirements: docs/requirements_1.txt
      - method: setuptools
        path: .

formats: []
